
version: 2

sources:
  - name: raw_road_traffic_incidents
    tables:
      - name: accident_information

models:
    - name: road_traffic_incidents
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
